# Backend API URL (without trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Add other environment variables here as needed
# These are example values - replace with your actual values in .env.local
